{
   "version": "0.2.0",
   "configurations": [
      {
         "name": "Launch: Late-Night-Programming",
         "args": ["--extensionDevelopmentPath=${workspaceFolder}"],
         "outFiles": ["${workspaceFolder}/out/**/*.js"],
         "request": "launch",
         "type": "extensionHost"
      }
   ]
}
